# Exercises

Exercises:

- Add a `Factorial` demo program. Create `src/FactorialApp` or add code to `ExercisesApp`.
- Add a `Student` class and serialize a list of students to JSON using `System.Text.Json`.
- Create a minimal API project exposing `GET /students` returning JSON.

Run the exercises placeholder:
```powershell
dotnet run --project src\ExercisesApp
```

When you're ready I can add full implementations for these exercises as separate projects.
